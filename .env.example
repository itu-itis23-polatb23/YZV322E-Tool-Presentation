# Postgres credentials — copy this file to .env and edit if needed
POSTGRES_USER=polars
POSTGRES_PASSWORD=polars_demo_2026
POSTGRES_DB=taxi
POSTGRES_HOST=postgres
POSTGRES_PORT=5432

# Jupyter token — visit http://localhost:8888 and paste this when prompted
JUPYTER_TOKEN=polars

# pgAdmin login — visit http://localhost:5050 and paste these
PGADMIN_EMAIL=admin@polars.demo
PGADMIN_PASSWORD=polars_demo_2026
